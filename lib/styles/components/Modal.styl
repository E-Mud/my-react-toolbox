@require '../flex.styl'
@require '../depth.styl'

.modal-mask
  position: fixed
  z-index: 9998
  top: 0
  left: 0
  width: 100%
  height: 100%
  background-color: rgba(0, 0, 0, .2)
  flex-container(center, center)

  .modal
    background-color: $white
    border-radius: 0.2rem
    z-depth(24)
